spring.application.name=Groundplay
spring.datasource.url=jdbc:postgresql://localhost:5432/groundplay
spring.datasource.username=postgres
spring.datasource.password=mypassword
spring.jpa.hibernate.ddl-auto=update

#logging.level.org.springframework=DEBUG

spring.output.ansi.enabled=always
logging.level.com.logging = TRACE
logging.pattern.console=%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){green} %clr([%level]){magenta}  %c{1.} [%t] %m%n
# # File OP
#logging.file.name = error.log
#logging.pattern.file= %clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){green} [%level] %c{1.} [%t] %m%n

# Security Configuration
# Now configured manually through SecurityConfig
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Encryption Configuration
encryption.secret=${ENCRYPTION_SECRET:defaultEncryptionSecretForDevelopmentEnvironmentOnly}

# JWT Configuration
jwt.secret=${JWT_SECRET:defaultJwtSecretForDevelopmentEnvironmentOnly12345678901234567890}